#!/usr/bin/env bash

sudo gitlab-rails runner "token = User.find_by_username('{{ gitlab_username }}').personal_access_tokens.create(scopes: [:api], name: 'MyToken'); token.set_token('{{ gitlab_api_token_user }}'); token.save"
